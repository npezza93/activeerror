<!DOCTYPE html>
<html class="active_error">
  <head>
    <title>Active error</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <style>
      .active_error body {
        background-color: #fff;
        color: #333;
        margin: 0;
      }

      .active_error body, .active_error p, .active_error ol, .active_error ul, .active_error td {
        font-family: verdana, arial, helvetica, sans-serif;
        font-size: 13px;
        line-height: 18px;
      }

      .active_error .button-as-link {
        border: none;
        text-decoration: underline;
        cursor: pointer;
        background: none;
        font-size: 15px;
        margin: 0;
        padding: 0;
        line-height: 18px;
      }
      .active_error a, .active_error .button-as-link {
        color: #000;
      }

      .active_error a:visited, .active_error .button-as-link:visited {
        color: #666;
      }

      .active_error a:hover, .active_error .button-as-link:hover {
        color: #fff;
        background-color: #000;
      }

      .active_error th {
        padding-bottom: 5px;
      }

      .active_error td {
        padding: 0 5px 7px;
        font-size: 15px;
      }

      .active_error #notice {
        color: #d3f0b7;
      }

      .active_error header {
        color: #f0f0f0;
        background: #c52f24;
        padding: 0.5em 1.5em;
        display: flex;
        flex-direction: row;
        margin-bottom: 0.83em;
      }

      .active_error h1 {
        flex: 1;
        margin: 0.2em 0;
        line-height: 1.1em;
        font-size: 2em;
      }

      .active_error table {
        padding: 0 1.5em;
        width: 100%;
      }

      .active_error table tr td:first-of-type {
        padding-left: 0;
      }

      .active_error .w-5 {
        width: 5%;
      }

      .active_error .w-15 {
        width: 15%;
      }
    </style>
  </head>
  <body><%= yield %></body>
</html>
